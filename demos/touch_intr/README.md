Interrupt Driven Touch Panel
----------------------------

This example uses the `EXTI` interrupt system to notice that things are
being touched and uses a double buffered structure to pass that information
back to the client.

